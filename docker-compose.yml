version: "3.8"
services:
  deletebot:
    container_name: deletebot
    build:
      context: .
      dockerfile: ./docker/Dockerfile.deletebot
    environment:
      - TOKEN=${TOKEN}
      - DBLTOKEN=${DBLTOKEN}
    dns:
      - 1.1.1.1
      - 1.0.0.1